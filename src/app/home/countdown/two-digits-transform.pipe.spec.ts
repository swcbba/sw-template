import { TwoDigitsTransformPipe } from './two-digits-transform.pipe';

describe('TwoDigitsTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TwoDigitsTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
