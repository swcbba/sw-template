import { ScrollSpyDirective } from './scroll-spy.directive';

describe('ScrollSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollSpyDirective(null);
    expect(directive).toBeTruthy();
  });
});
